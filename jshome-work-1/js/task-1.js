// const generatorName = "Генератор захисного поля";
// let generatorPrice = 1000;

// console.log('обрано' + ' "' + generatorName + '" ' + 'ціна за штуку' + ' ' + generatorPrice + ' ' + 'кредитів');

// generatorPrice = 2000;

// console.log(`обрано '${generatorName}' ціна за штуку ${generatorPrice} кредитів`);
